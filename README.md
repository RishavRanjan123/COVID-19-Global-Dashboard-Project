# Movie Search App

A React app that searches movies via the OMDB API and lets you save/remove favorites, persisted in `localStorage`.

## Features
- Search movies by title
- Add/remove favorites
- Favorites persist across sessions via `localStorage`

## Tech
React, Bootstrap

## Run locally
```
npm install
npm start
```
